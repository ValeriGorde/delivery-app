@model Delivery.Web.Models.OrderViewModel
@{
    string title = "View Order " + ViewData["Id"];
    ViewData["Title"] = title;
}

@if (Model is null)
{
    <div>Заказ не найден</div>
}
else
{
    <div>
        <dl class="dl-horizontal">
            <dt>Идентификатор</dt>
            <dd>@Model.Id</dd>
            <dt>Город отправителя</dt>
            <dd>@Model.SendersCity</dd>
            <dt>Адрес отправителя</dt>
            <dd>@Model.SendersAddress</dd>
            <dt>Город получателя</dt>
            <dd>@Model.RecipientCity</dd>
            <dt>Адрес получателя</dt>
            <dd>@Model.RecipientAddress</dd>
            <dt>Вес груза</dt>
            <dd>@Model.CargoWeight</dd>
            <dt>Дата забора груза</dt>
            <dd>@Model.DatePickup</dd>
        </dl>
    </div>

    <a asp-controller="Order" asp-action="Index">Вернуться к списку заказов</a>
    @* <div class="container p-3">
        <div class="row pt-4">
            <div class="col-md-6 offset-md-3">
                <form method="POST">
                    <legend>Просмотр заказа</legend>
                    <div class="mb-3">
                        @Html.DisplayFor(modelItem => Model.Id)
                    </div>
                    <div class="mb-3">
                        @Html.DisplayFor(modelItem => Model.SendersCity)
                    </div>
                    <div class="mb-3">
                        @Html.DisplayFor(modelItem => Model.SendersAddress)
                    </div>
                    <div class="mb-3">
                        @Html.DisplayFor(modelItem => Model.RecipientCity)
                    </div>
                    <div class="mb-3">
                        @Html.DisplayFor(modelItem => Model.RecipientAddress)
                    </div>
                    <div class="mb-3">
                        @Html.DisplayFor(modelItem => Model.CargoWeight)
                    </div>
                    <div class="mb-3">
                        @Html.DisplayFor(modelItem => Model.DatePickup)
                    </div>
                    <div class="mb-3 pt-3">
                        <a asp-controller="Order" asp-action="Index">Вернуться к списку заказов</a>
                    </div>
                </form>
            </div>
        </div>
    </div> *@
}


